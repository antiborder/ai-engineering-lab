rules_version = '2';

// Local emulator only — Phase 2 (real Firestore) will define proper
// least-privilege rules once auth exists (spec section 39).
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
